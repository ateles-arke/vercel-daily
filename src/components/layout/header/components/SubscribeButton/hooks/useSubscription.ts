/**
 * Backward-compatible re-export for the shared subscription hook.
 * Existing component-local imports can continue to resolve without duplicating logic.
 */
export { useSubscription } from '@/hooks/useSubscription';
